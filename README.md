# X Keyword Monitor (Starter)

This is a starter template.

## Setup
1. Copy `.env.example` to `.env`
2. Fill in your Telegram bot token and chat ID.
3. Run:
   npm install
   npm start

Replace `src/monitor.js` with your monitoring implementation.
